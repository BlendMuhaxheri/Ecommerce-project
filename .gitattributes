# Treat Blade as PHP (not its own language weight)
*.blade.php linguist-language=PHP

# Optional: ensure frontend is counted properly
frontend/** linguist-detectable=true

# If frontend is Next.js/JS/TS heavy, you can also force clarity
frontend/** linguist-language=JavaScript